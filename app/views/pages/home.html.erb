  <div class="home-header">
    <div class="home-header-background">
    </div>
    <div class="container-headline">
      <h1 class="headline">Créer du lien entre Nature et Humains</h1>
    </div>
  </div>
<div class="container">
  <%= render 'shared/intro' %>
  <h2 class="home-h2">Nos animations</h2>
    <div class="marginblock30">
      <% @intro_activity.each do |i| %>
      <%= i.teaser %>
      <% end %>
    </div>
    <%= render 'shared/animations' %>
    <h2 class="home-h2">Nos produits</h2>
    <div class="marginblock30">
      <% @intro_product.each do |i| %>
      <%= i.teaser %>
      <% end %>
    </div>
    <%= render 'shared/products' %>

</div>

