<div class="container margin-top">
  <h1>Modifier mon affiliation</h1>
  <hr>
  <%= simple_form_for @membership, url: membership_path, method: :patch  do |t| %>
  <%= t.input :start_date, as: :string, required: true, label: 'Date de début', input_html: {class: "datepicker"} %>
  <%= t.input :price, label: 'Montant', input_html: { min: 1 } %>
  <%= t.submit "Enregistrer", class: "btn btn-primary" %>
  <% end %>
</div>
