<div class="container margin-top">
    <div class="row">
    <div class="col-8">
      <h1 class="title-admin">Gestion des affiliations</h1>
    </div>
    <div class="col-4">
      <%= link_to 'Ajouter une affiliation', new_membership_path, class:'btn btn-primary btn-right' %>
    </div>
  </div>
  <hr>
    <% @memberships.each do |t| %>
      <div class="row admin-row">
        <div class="col">
         <b><%= t.user.firstname %> <%= t.user.lastname %> </b>
        </div>
        <div class="col-2">
          <%= t.start_date %>
        </div>
        <div class="col-2">
          <%= t.price %> €
        </div>
      <div class="col-2">
        <%= link_to edit_membership_path(t.id), title:"Modifier", class: "btn btn-primary" do %>
          Modifier
        <% end %>
      </div>
      <div class="col-1">
              <%= button_to "Supprimer", membership_path(t.id),
                  method: :delete,
                  data: { confirm: "Veuillez confirmer la suppression !" },
                 :id => "delete", class:"btn btn-danger" %>
      </div>
      </div>
    <% end %>
</div>
