<div class="container margin-top">
  <h1>Je soutiens Terre@Air !</h1>
  <hr>
  <%= simple_form_for @sponsorship, url: sponsorships_path do |f| %>
  <%= f.input :recurring, as: :checkboxes, label: "Renouvellement automatique" %>
  <%= f.input :frequency, label: 'Fréquence', collection: ['annuel', 'trimestriel', 'mensuel'] %>
  <%= f.input :amount, label: 'Montant (en euro)' %>
  <%= f.submit "Enregistrer", class: "btn btn-primary" %>
  <% end %>
</div>
