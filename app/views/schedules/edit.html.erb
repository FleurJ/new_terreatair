<div class="container margin-top">
  <h1>Modifier le nouveau créneau de réservation</h1>
  <hr>
  <%= simple_form_for @schedule, url: schedule_path, method: :patch do |f| %>
  <%= f.association :activities, as: :check_boxes, collection: @activities, label: 'Activité' %>

  <%= f.input :schedule_date, order: [:day, :month, :year], label: "Date" %>
  <%= f.input :classes_qty, label: 'Nombre de classes' %>
  <% if current_user.admin == true %>
  <%= f.input :status, label: 'Statut', collection: ['open', 'closed', 'booked'] %>
  <% end %>
  <%= f.submit "Enregistrer", class:"btn btn-primary" %>
  <% end %>
</div>
