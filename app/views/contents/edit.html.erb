<div class="container">
  <h1>Modifier : <i><%= @content.title %></i></h1>
  <hr>
  <%= simple_form_for @content, url: content_path, method: :patch do |c| %>
  <%= c.input :title, label: 'Titre' %>

  <%= c.input :teaser, label: 'Chapeau - Teaser' %>
  <%= c.input :status, label: 'Statut', collection: ['draft', 'published', 'archived'] %>

    <%= c.submit "Enregistrer", class:"btn btn-primary" %>
  <% end %>
</div>
<script src="application.js"></script>
