<div class="container margin-top">
  <h1>Enregistrer une nouvelle adresse</h1>
  <hr>
  <%= simple_form_for @address, url: addresses_path do |f| %>
  <%= f.input :address_type, as: :radio_buttons, label: "Type d'adresse", collection: ['facturation', 'livraison']  %>
  <%= f.input :first_name, label: 'Nom' %>
  <%= f.input :last_name, label: 'Prénom' %>
  <%= f.input :company, label: 'Entreprise' %>
  <%= f.input :address1, label: 'Adresse' %>
  <%= f.input :address2, label: false %>
  <%= f.input :zipcode, label:'Code postal' %>
  <%= f.input :city, label:'Localité' %>
  <%= f.input :country, as: :string, label: 'Pays' %>
  <%= f.submit "Enregistrer", class: "btn btn-primary" %>
  <% end %>
</div>
